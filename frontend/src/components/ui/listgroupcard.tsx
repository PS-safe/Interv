import * as React from "react"
import { cn } from "@/lib/utils"
import { GroupCardProps } from "./groupcard"

type ListGroupCardProps = {
    card: GroupCardProps[]
    x?: number[]
}



const ListGroupCard: React.FC<ListGroupCardProps> = ({
    card,

}) => {
    return (
        <div>

        </div>
    )
}

export default ListGroupCard;